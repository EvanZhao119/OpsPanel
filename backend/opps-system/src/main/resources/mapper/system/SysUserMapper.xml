<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opspanel.system.mapper.SysUserMapper">

    <resultMap id="SysUserMap" type="com.opspanel.system.domain.SysUser">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="nick_name" property="nickName"/>
        <result column="password" property="password"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="status" property="status"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <select id="selectList" resultMap="SysUserMap">
        SELECT * FROM sys_user
        <where>
            <if test="q.username != null and q.username != ''">
                AND username LIKE CONCAT('%', #{q.username}, '%')
            </if>
            <if test="q.nickName != null and q.nickName != ''">
                AND nick_name LIKE CONCAT('%', #{q.nickName}, '%')
            </if>
            <if test="q.status != null">
                AND status = #{q.status}
            </if>
            <if test="q.deptId != null">
                AND dept_id = #{q.deptId}
            </if>
            AND deleted = false
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectById" resultMap="SysUserMap">
        SELECT * FROM sys_user WHERE user_id = #{userId} AND deleted = false
    </select>

    <select id="selectByUsername" resultMap="SysUserMap">
        SELECT * FROM sys_user WHERE username = #{username} AND deleted = false
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO sys_user(username, nick_name, password, email, phone, dept_id, status, deleted, create_time)
        VALUES(#{username}, #{nickName}, #{password}, #{email}, #{phone}, #{deptId}, #{status}, false, NOW())
    </insert>

    <update id="update">
        UPDATE sys_user
        SET nick_name = #{nickName}, email = #{email}, phone = #{phone},
        dept_id = #{deptId}, status = #{status}, update_time = NOW()
        WHERE user_id = #{userId} AND deleted = false
    </update>

    <delete id="deleteById">
        UPDATE sys_user SET deleted = true WHERE user_id = #{userId}
    </delete>

</mapper>
